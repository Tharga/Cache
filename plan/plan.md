# Plan: blazor-ui-overhaul

## Steps
- [~] 1. Add `PersistType` to `CacheTypeInfo` and populate in `CacheMonitor`
- [ ] 2. Slim `ListView.razor` columns + persist type column + chevron button
- [ ] 3. Create `DetailView.razor` dialog with metadata + JSON + CopyButton
- [ ] 4. Wire up live refresh via `DataSetEvent`/`DataDropEvent` in SummaryView and ListView
- [ ] 5. Build, run tests, commit
